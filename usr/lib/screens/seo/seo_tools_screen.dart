import 'package:flutter/material.dart';

class SEOToolsScreen extends StatelessWidget {
  const SEOToolsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SEO Tools'),
      ),
      body: const Center(
        child: Text('SEO Tools Screen - Coming Soon!'),
      ),
    );
  }
}
